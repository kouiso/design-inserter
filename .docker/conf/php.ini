upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 256M
max_execution_time = 300
max_input_vars = 3000
date.timezone = Asia/Tokyo
; wp-cli 2.12 が PHP 8.5 で出す Deprecated 通知が標準出力を汚染し、
; `wp post create --porcelain` 等の出力パースを壊すため stderr に退避する
display_errors = stderr
