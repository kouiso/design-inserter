version: '3'

tasks:
  ci:fast:
    cmds:
      - npm ci
      - npm run phpcs
      - npm run php:lint
      # git-crypt ロック環境でもゲートを通す。リリース zip は復号済み環境の npm run build が担当。
      - npm run build:dev
      - npm test
      - npm run test:php

  ci:
    cmds:
      - task: ci:fast
